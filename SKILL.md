---
name: daily-finance-report
description: 生成猫笔刀风格的全球财经日报（含宏观观点、A股/港股股票推荐、ETF实操框架、深度观察、核心行情数据），并推送到微信。触发场景：用户说「生成/更新财经日报」「跑今天的日报」「推荐股票」「推荐ETF」「今日市场怎么看」「全球财经新闻分析」「夜盘观察」等。当用户提到每日财经新闻整理、股票/ETF推荐、市场深度分析、微信推送日报时，务必使用本技能，即使没有明确说「日报」二字。
---

# 全球财经日报生成器

生成一份**可执行、可回溯、猫笔刀风格**的全球财经日报，并推送到用户微信（Server酱）。
本技能将采集、分析、推荐、推送的全流程封装为可复用脚本，避免每次从零开始。

## 核心原则

1. **为个人实盘投资服务**：所有推荐必须可执行（含目标价/区间、止盈止损、配置权重），不写空话。
2. **连贯性优先**：先读推荐跟踪档案，延续已有推荐逻辑，不每天换标的。
3. **诚实优先**：数据必须真实（用脚本拉取），拿不到就留空，不估算、不编造。
4. **风格统一**：诙谐犀利、节奏明快、善用表情符号（💡⚠️🚀）、避免术语堆砌。

## 工作流程

### 第1步：采集数据

```bash
# 新闻采集（CNBC/MW RSS + 财新 + 一财）
python3 scripts/collect_news.py --limit 10

# 行情采集（腾讯行情API + 汇率 + 黄金）
python3 scripts/fetch_quotes.py
```

- 新闻脚本返回 JSON，提取标题、来源、摘要。
- 行情脚本返回各指标价格/涨跌幅/时间，A股指数收盘后时间显示为「15:00(收盘)」。
- 任何接口超时/失败：跳过该源，不阻塞流程。

### 第2步：读取推荐跟踪档案

```bash
TRACK_FILE="AI 工作区/成果文件/全球财经日报/推荐跟踪档案.md"
if [ -f "$TRACK_FILE" ]; then cat "$TRACK_FILE"; fi
```

历史推荐是连贯性的基础——先看推过什么、盈亏如何、逻辑是否还成立。

### 第3步：确定日期

```bash
DATE=$(date +%Y-%m-%d)
# 周末用最近交易日数据
WEEKDAY=$(date +%u)
if [ "$WEEKDAY" -gt 5 ]; then
  TRADE_DATE=$(date -d "last friday" +%Y-%m-%d)
else
  TRADE_DATE=$DATE
fi
```

### 第4步：撰写日报

读取 `references/report_template.md` 获取完整模板，**严格按模板结构输出**。
读取 `references/recommendation_rules.md` 获取推荐规则，**严格遵守**。

关键规则速览（详见 references/recommendation_rules.md）：
- 股票仅限 A股/港股；A+H 双上市默认选 H 股（算溢价率）
- 股票 ≤2只，ETF ≤2只，没有把握就写「暂无明确推荐」
- 目标价偏离当前价 ≤30%，必须用行情API核实
- ETF 必须含：核心逻辑、配置权重、目标区间、止盈止损、逻辑依据、跟踪记录

### 第5步：更新跟踪档案

在 `推荐跟踪档案.md` 末尾追加今日推荐记录（股票和ETF都记录）：
```bash
echo "| ${DATE} | [代码] | [名称] | [推荐价] | [目标价/区间] | [止盈止损] | — | [逻辑摘要+权重建议]" >> "$TRACK_FILE"
```
已有推荐的价格变化时，同步更新最新价和盈亏。

### 第6步：保存与推送

```bash
# 保存日报
# 文件路径：AI 工作区/成果文件/全球财经日报/${DATE}-完整日报.md

# 推送微信
python3 scripts/push_wechat.py "AI 工作区/成果文件/全球财经日报/${DATE}-完整日报.md"
```

推送成功后向用户报告：文件保存路径 + 推送状态。

## 日报结构（五章节）

1. **💡 核心观点** — 3-5句带态度的市场要点
2. **🚀 股票推荐** — ≤2只，含逻辑/跟踪记录/目标价/风险
3. **📊 ETF推荐** — ≤2只，含完整实操框架
4. **🌍 深度观察** — 3-6条猫笔刀风格分析（事实→量化锚点→影响层级→短期推演→中长期逻辑链）
5. **📊 核心数据** — 16项指标行情表（留空不显示无数据项）
6. **⚡ 今日关注清单** — 3-5个关键事件/逻辑链

## 脚本说明

| 脚本 | 功能 | 输出 |
|------|------|------|
| `scripts/collect_news.py` | 新闻采集 | JSON 数组 |
| `scripts/fetch_quotes.py` | 行情采集 | JSON 对象 |
| `scripts/push_wechat.py` | Server酱推送 | 推送结果 |

## 参考文件

- `references/report_template.md` — 日报完整模板（撰写时必读）
- `references/recommendation_rules.md` — 股票/ETF推荐规则（推荐时必读）
- `assets/track_template.md` — 跟踪档案格式说明

## 免责声明

所有推荐基于公开信息生成，不构成投资建议。投资有风险，入市需谨慎。
